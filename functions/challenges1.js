//Challenge 2 - Create a function that takes a number as a parameter and converts it to a string.
const numToString = (num) => {
    return num.toString()
}
console.log(numToString(4))
console.log(typeof(numToString(4)))

//Challenge 3 - Create a function that takes a number as a parameter and adds one.
const addOne = (num) => {
    return num + 1
}
console.log(addOne(3))

//Challenge 4 - Create a function that takes a number as a parameter and takes one off.
const minusOne = (num) => {
    return num - 1
}
console.log(minusOne(3))

//Challenge 5 - Create a function that takes two numbers as a parameters and adds them together.
const addTogether = (num1, num2) => {
    return num1 + num2
}
console.log(addTogether(3, 4))